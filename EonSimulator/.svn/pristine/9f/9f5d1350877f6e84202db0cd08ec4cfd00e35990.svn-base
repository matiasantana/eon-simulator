package br.ufpe.eonsimulator.math;

import org.junit.Test;

import br.ufpe.simulator.math.functions.RoVariable;

public class RoVariableTest {

	@Test
	public void test() {
		RoVariable roVariable = new RoVariable(0.22 / 4.34, 100, 20.7, 0);
		System.out.println(roVariable.value(3 * Math.pow(10, 11),
				3 * Math.pow(10, 11), 2.75 * Math.pow(10, 11)));
	}
}
